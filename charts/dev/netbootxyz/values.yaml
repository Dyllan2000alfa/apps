image:
  repository: tccr.io/truecharts/netbootxyz
  pullPolicy: IfNotPresent
  tag: v2.0.53

env:
  PUID: 568

service:
  main:
    ports:
      main:
        targetPort: 3000
        port: 3000

persistence:
  varrun:
    enabled: true
