image:
  repository: tccr.io/truecharts/papermerge
  pullPolicy: IfNotPresent
  tag: v2.0.1

env:
  PUID: 568

service:
  main:
    ports:
      main:
        targetPort: 8000
        port: 8000

persistence:
  varrun:
    enabled: true
