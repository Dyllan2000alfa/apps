image:
  repository: tccr.io/truecharts/fleet
  pullPolicy: IfNotPresent
  tag: v2.3.1

env:
  PUID: 568

service:
  main:
    ports:
      main:
        targetPort: 8080
        port: 8080

persistence:
  varrun:
    enabled: true
