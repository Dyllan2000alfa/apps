image:
  repository: tccr.io/truecharts/hedgedoc
  pullPolicy: IfNotPresent
  tag: v1.9.2

env:
  PUID: 568

service:
  main:
    ports:
      main:
        targetPort: 3000
        port: 3000

persistence:
  varrun:
    enabled: true
