image:
  repository: tccr.io/truecharts/mysql-workbench
  pullPolicy: IfNotPresent
  tag: mysql-workbench

env: {}

service:
  main:
    ports:
      main:
        targetPort: 3000
        port: 3000
