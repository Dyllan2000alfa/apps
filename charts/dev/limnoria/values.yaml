image:
  repository: tccr.io/truecharts/limnoria
  pullPolicy: IfNotPresent
  tag: v2021.11.20

env:
  PUID: 568

service:
  main:
    ports:
      main:
        targetPort: 8080
        port: 8080

persistence:
  varrun:
    enabled: true
