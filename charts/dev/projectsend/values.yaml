image:
  repository: tccr.io/truecharts/projectsend
  pullPolicy: IfNotPresent
  tag: v2021.12.10

env:
  PUID: 568

service:
  main:
    ports:
      main:
        targetPort: 80
        port: 80

persistence:
  varrun:
    enabled: true
